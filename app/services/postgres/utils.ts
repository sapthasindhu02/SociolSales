export const handleTransriptionData = async (colName: string) => {
    //if Narayanapattu column(row in SareesArrangement) already exists increase count of sarees else add new row in table SareesArrangement
    //with default count 1 and create Narayanapattu table with two cols image, s.no will be the row number of saree in shop 
    //open Narayanapattu table and add image to table
    
}